import React from "react";
import { Text, StyleSheet } from "react-native";
import { Button } from "react-native-elements";

const Map = () => {
  return <Text>I Map</Text>;
};

const styles = StyleSheet.create({});
export default Map;
